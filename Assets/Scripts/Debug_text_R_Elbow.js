#pragma strict

var debugRElbow:TextMesh;
debugRElbow = gameObject.GetComponent(TextMesh);
debugRElbow.text =  "";

function Update () {

	//debugRElbow.text =  BoneColor.relbow.ToString();
	//debugRElbow.text =  "bot RE = " + (BoneColor.relbow.ToString()) + ", p RE = " + (BoneColor.playerOffset.ToString());
}